import { createFileRoute, Link } from "@tanstack/react-router";
import { MessageCircle, Plus, ThumbsUp } from "lucide-react";
import { useCommunityPosts } from "@/lib/community";
import { absoluteUrl } from "@/lib/site";
export const Route = createFileRoute("/community/")({ head: () => ({ meta: [{ title: "Community | FishTankr" }, { name: "description", content: "Aquarium questions, tank photos, ideas and practical fishkeeping discussion." }], links: [{ rel: "canonical", href: absoluteUrl("/community") }] }), component: CommunityFeed });
function CommunityFeed() { const posts = useCommunityPosts(); return <main className="mx-auto grid max-w-7xl gap-8 px-4 py-8 sm:px-6 lg:grid-cols-[minmax(0,1fr)_280px]"><section><header className="flex flex-wrap items-end justify-between gap-4 border-b pb-6"><div><p className="science-label text-primary">FishTankr community</p><h1 className="mt-2 font-display text-4xl font-bold text-foreground">Talk tanks.</h1><p className="mt-2 text-sm text-muted-foreground">Questions, photos, setups, gear, fish and all the bits in between.</p></div><Link to="/community/new" className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground"><Plus className="size-4" />Create post</Link></header><div className="mt-5 flex gap-2 overflow-auto text-sm font-semibold"><span className="rounded-full bg-foreground px-4 py-2 text-background">Hot</span><span className="rounded-full bg-muted px-4 py-2 text-muted-foreground">New</span><span className="rounded-full bg-muted px-4 py-2 text-muted-foreground">Top</span><span className="rounded-full bg-muted px-4 py-2 text-muted-foreground">Unanswered</span></div>{posts.isLoading ? <p className="py-16 text-center text-sm text-muted-foreground">Loading the feed...</p> : posts.data?.length ? <div className="mt-4 space-y-3">{posts.data.map((post) => <article key={post.id} className="grid grid-cols-[42px_minmax(0,1fr)] gap-3 rounded-xl border bg-card p-4"><div className="flex flex-col items-center gap-1 text-muted-foreground"><ThumbsUp className="size-4" /><span className="text-xs font-bold">{post.score}</span></div><div><div className="flex flex-wrap items-center gap-2 text-xs"><span className="rounded-full bg-primary/10 px-2 py-1 font-bold text-primary">{post.flair}</span><span className="text-muted-foreground">u/{post.author_handle} · {new Date(post.created_at).toLocaleDateString()}</span></div><Link to="/community/p/$slug" params={{ slug: post.slug }} className="mt-2 block font-display text-xl font-bold text-foreground hover:text-primary">{post.title}</Link><p className="mt-2 line-clamp-3 text-sm leading-relaxed text-muted-foreground">{post.body}</p><div className="mt-4 flex gap-4 text-xs font-semibold text-muted-foreground"><span className="inline-flex items-center gap-1"><MessageCircle className="size-4" />{post.comment_count} comments</span><span>Share</span><span>Save</span></div></div></article>)}</div> : <div className="mt-6 rounded-xl border border-dashed p-10 text-center"><h2 className="font-display text-2xl font-bold">Be the first to start a good thread.</h2><p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">Show your tank, ask a question, or share something that made your fishkeeping easier.</p><Link to="/community/new" className="mt-5 inline-flex rounded-lg bg-primary px-4 py-2 text-sm font-bold text-primary-foreground">Create a post</Link></div>}</section><aside className="h-fit rounded-xl border bg-card p-5"><h2 className="font-display text-xl font-bold">A good community tank</h2><p className="mt-3 text-sm leading-relaxed text-muted-foreground">Be specific, be kind, and give the reason behind your advice. Serious fish illness or injury needs an aquatic vet.</p><Link to="/tank-ideas" className="mt-5 block text-sm font-bold text-primary">Browse tank ideas</Link></aside></main>; }
